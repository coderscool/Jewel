namespace JewelPainter.UI.Definitions
{
    /// Loại kho lượt mà popup mời thêm lượt đang nói tới.
    public enum CreditPoolKind
    {
        Hint = 0,

        FreePaint = 1,

        FillColor = 2,
    }
}
